# Backend API base URL (includes /api prefix). Required for all browser and App Router fetches.
# The typed client in lib/api/client.ts is the only place that reads this variable.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
